@model StockFlow.ViewModels.Customers.CustomerInputModel

@{
    ViewData["Title"] = "Yeni müşteri";
}

<div class="page-heading">
    <div>
        <p class="page-eyebrow">MÜŞTERİ YÖNETİMİ</p>
        <h1>Yeni müşteri</h1>
        <p class="page-intro">Müşteri adını ve varsa iletişim bilgilerini kaydedin.</p>
    </div>
</div>

<section class="management-panel form-panel" aria-labelledby="create-customer-title">
    <h2 id="create-customer-title" class="visually-hidden">Müşteri oluşturma formu</h2>
    <form asp-action="Create" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>
        <partial name="_FormFields" model="Model" />
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Müşteriyi oluştur</button>
            <a asp-action="Index" class="btn btn-outline-secondary">İptal</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
